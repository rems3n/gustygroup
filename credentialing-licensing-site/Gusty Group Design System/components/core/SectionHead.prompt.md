**SectionHead** — the large serif H2 + intro that opens every section.

```jsx
<SectionHead
  title="Situations we're built for."
  intro="If one of these sounds like your challenge, we should talk." />
```

Use `tone="on-dark"` inside olive sections.
