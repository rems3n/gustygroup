**Tag** — small sans meta label above headlines and case studies.

```jsx
<Tag>Flagship · 2018 — 2026</Tag>
<Tag tone="on-dark">Selected work</Tag>
```

Tones: `muted` (default, ink-dim), `on-dark` (translucent white for olive surfaces), `accent` (olive).
