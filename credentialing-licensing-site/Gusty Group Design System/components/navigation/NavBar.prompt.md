**NavBar** — the olive sticky header. Rendered-type "GUSTY / GROUP" wordmark (no logo file exists), a link list, and an `on-dark` outlined CTA. Composes Button.

```jsx
<NavBar ctaLabel="Contact" ctaHref="#book" />
```
