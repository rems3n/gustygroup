**FaqItem** — native `<details>` row: serif question, `+` marker, sans answer, hairline rules. Stack them; the first/last get an ink rule via the container.

```jsx
<FaqItem question="How do engagements typically work?">
  After aligning on scope, we embed with your team…
</FaqItem>
```
