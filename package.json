{
  "name": "gustygroup-site",
  "private": true,
  "type": "module",
  "description": "Static site for gustygroup.com. Only the /cred blog + resources are generated; every other page is hand-authored HTML with no build step.",
  "scripts": {
    "build:blog": "node scripts/build-blog.mjs",
    "serve": "python3 -m http.server 8080"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
